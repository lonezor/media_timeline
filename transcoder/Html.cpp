#include "Html.h"

/*-------------------------------------------------------------------------------------------------------------------*/

Html::Html() {
	page = "";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::docTypeHtml() {
	page += "<!DOCTYPE html>\n";
}

void Html::beginHtml() {
	page += "<html>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endHtml() {
	page += "</html>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginHeader() {
	page += "<head>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/
void Html::endHeader() {
	page += "</head>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginStyle() {
	page += "<style type=\"text/css\">\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endStyle() {
	page += "</style>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::addLink(string rel, string href) {
	page += "<link rel='" + rel + "' href='" + href + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::addScript(string src) {
	page += "<script src='" + src + "'></script>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginScript() {
	page += "<script type=\"text/javascript\">\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginScript(string language) {
	page += "<script language='" + language + "'>\n";
}
/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endScript() {
	page += "</script>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginCssDeclaration(string name) {
	page += name + " {\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endCssDeclaration() {
	page += "}\n\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::addCssEntry(string key, string value) {
	page += key + ": " + value + ";\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginBody() {
	page += "<body>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endBody() {
	page += "</body>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/
void Html::beginDiv() {
	page += "<div>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginDiv(string divClass) {
	page += "<div class='" + divClass + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginDiv(string divClass, string tabIndex, string role, string ariaHidden) {
	page += "<div class='" + divClass + "' tabindex='" + tabIndex + "' role='" + role + "' aria-hidden='" + ariaHidden + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginDivImageGallery(string divClass) {
	page += "<div class='" + divClass + "' itemscope itemtype='http://schema.org/ImageGallery'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endDiv() {
	page += "</div>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

//void Html::beginButton(string divClass, string title) {
//	page += "<button class='" + divClass + "' title='" + title + "'>\n";
//}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginButton(string id, string btnClass, string onClick) {
	page += "<button id='" + id + "' class='" + btnClass + "' onclick='" + onClick + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endButton() {
    page += "</button>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/
void Html::beginTable() {
	page += "<table>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/
void Html::beginTable(string style) {
	page += "<table style='" + style + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endTable() {
	page += "</table>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginTr() {
	page += "<tr>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endTr() {
	page += "</tr>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginTd() {
	page += "<td>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginTd(string width) {
	page += "<td width='" + width + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endTd() {
	page += "</td>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginB() {
	page += "<b>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endB() {
	page += "</b>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginForm(string action, string method, string enctype) {
	page += "<form action='" + action + "' method='" + method + "' enctype='" + enctype + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endForm() {
	page += "</form>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::addInput(string type, string name) {
	page += "<input type='" + type + "' name='" + name + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::addInput(string type, string name, string value) {
	page += "<input type='" + type + "' name='" + name + "' value='" + value + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::addInputMultiple(string type, string name) {
	page += "<input type='" + type + "' name='" + name + "' multiple>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginH1() {
	page += "<h1>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginH1(string dataTitle) {
	page += "<h1 data-title='" + dataTitle + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endH1() {
	page += "</h1>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginH2() {
	page += "<h2>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endH2() {
	page += "</h2>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginH3() {
	page += "<h3>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endH3() {
	page += "</h3>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginA(string href) {
	page += "<a href='" + href + "'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginA(string href, string aClass) {
	page += "<a href='" + href + "' class='" + aClass + "'>\n";
}

void Html::beginAImageGallery(string href) {
	page += "<a href='" + href + "' itemprop='contentUrl' data-size='2048x1536'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endA() {
	page += "</a>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::addImgImageGallery(string src, string alt) {
	page += "<img src='" + src + "' itemprop='thumbnail' alt='" + alt + "'/>\n";
}

void Html::addImg(string src, string alt, string width, string height) {
	page += "<img src='" + src + "' alt='" + alt + "' width='" + width + "' height='" + height + "'/>\n";
}

void Html::addImgOnClick(string onClick, string src) {
	page += "<img onclick=\"" + onClick + "\" src=\"" + src + "\"/>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::addText(string text) {
	page += text + "\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginUl() {
	page += "<ul>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endUl() {
	page += "</ul>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginLi() {
	page += "<li>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endLi() {
	page += "</li>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginFigure() {
	page += "<figure itemprop='associatedMedia' itemscope itemtype='http://schema.org/ImageObject'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endFigure() {
	page += "</figure>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginFigCaption() {
	page += "<figcaption itemprop='caption description'>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endFigCaption() {
	page += "</figcaption>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::beginVideo(string id, string width, string height, bool controls) {
	string ctrl = "";
	if (controls) {
		ctrl = " controls";
	}
	page += "<video id='" + id + "' width='" + width + "' height='" + height + "'" + ctrl + ">\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::endVideo() {
	page += "</video>\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::addVideoSrc(string errorMsg) {
	page += errorMsg + "\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

void Html::addVideoSrc(string src, string type) {
	page += "<source src='" + src + "' type='" + type + "'\n";
}

/*-------------------------------------------------------------------------------------------------------------------*/

